# --- hypridle desactivado por completo: sin bloqueo, sin clave al despertar, sin idle ---
# general {
#     lock_cmd = omarchy-system-lock
#     before_sleep_cmd = OMARCHY_LOCK_ONLY=true omarchy-system-lock
#     after_sleep_cmd = sleep 1 && omarchy-system-wake
#     inhibit_sleep = 3
# }

# --- Inactividad desactivada a propósito (no bloquea ni lanza protector) ---
# La suspensión MANUAL sigue bloqueando antes de dormir (ver bloque general).
# Para reactivar el bloqueo por inactividad, descomenta los dos listeners.

# Start screensaver after 2.5 minutes
# listener {
#     timeout = 150
#     on-timeout = pidof hyprlock || omarchy-launch-screensaver
# }

# Lock system after 5 minutes (screensaver resets idle timer, so have to just do half + 2s margin)
# listener {
#     timeout = 152
#     on-timeout = omarchy-system-lock
#     on-resume = omarchy-system-wake
# }
