# Repo-only files: never applied to $HOME.
README.md
CLAUDE.md
bootstrap.sh
packages.txt
packages-aur.txt
themes.txt
av.png


{{ if not .hybrid_gpu -}}
# AQ_DRM_DEVICES only makes sense on the hybrid AMD+NVIDIA laptop.
.config/uwsm/env-hyprland
{{- end }}
